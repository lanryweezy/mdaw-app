## 2024-05-18 - Added Tooltips to Icon-Only Buttons
**Learning:** Icon-only buttons without tooltips are a common accessibility issue. Adding dynamic tooltips based on the button state improves accessibility for screen readers.
**Action:** Always add `tooltip` properties to `IconButton` widgets, especially when they only contain an `Icon` without a text label.
